# TrackStockz Client Environment Configuration

# --- Backend Connection ---
# Select the appropriate API URL based on your environment.

# Development:
# Uncomment the line below when running the server locally (default port 8000).
# VITE_BACKEND_URL="http://localhost:8000"

# Production:
# The URL of your deployed backend service (e.g., Render, AWS).
VITE_BACKEND_URL="YOUR_BACKEND_URL"

# --- Authentication ---
# Google OAuth Client ID
# Required for "Sign in with Google" functionality. 
# Obtain this from your Google Cloud Console Credentials.
VITE_GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_ID"
